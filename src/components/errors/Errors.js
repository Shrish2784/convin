import React from "react";
import "./Errors.scss";

export default function Errors(props) {
    return (
        <div id="errors">
            There can not be any spaces in the Domain Name.
        </div>
    );
}